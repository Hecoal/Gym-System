package Sistema;

/**
 * This java interface creates an interface, and allows you to calculate[...]
 * [...]the payment in a specified time
 * @author Hecoal
 */
public interface Payment {
    
    void calculatePayment();
}
